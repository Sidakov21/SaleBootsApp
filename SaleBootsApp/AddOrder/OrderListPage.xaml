<Page x:Class="SaleBootsApp.AddOrder.OrderListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SaleBootsApp.AddOrder"
      Title="Список заказов"
      Background="{StaticResource MainBackground}"
      Loaded="OrderListPage_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Margin="10" Orientation="Horizontal">
            <TextBlock Text="Список Заказов" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,20,0"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Добавить заказ" x:Name="AddOrderButton" Click="AddOrderButton_Click" 
                        Width="120" Height="30" Margin="0,0,10,0" Background="{StaticResource AccentColor}" Visibility="Collapsed"/>

                <Button Content="Удалить заказ" x:Name="DeleteOrderButton" Click="DeleteOrderButton_Click" 
                        Width="120" Height="30" Margin="0,0,10,0" Background="Red" Visibility="Collapsed"/>

                <Button Content="Назад к товарам" Click="GoToProductsButton_Click" 
                        Width="150" Height="30" Background="{StaticResource SecondaryBackground}"/>
            </StackPanel>
        </StackPanel>

        <ListView Grid.Row="1" x:Name="OrderListView" Margin="10" 
                  MouseDoubleClick="OrderListView_MouseDoubleClick">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="1" Padding="5" Margin="5" Width="800">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock FontWeight="Bold" Text="{Binding OrderArticle, StringFormat='Артикул заказа: {0}'}"/>
                                <TextBlock Text="{Binding StatusName, StringFormat='Статус заказа: {0}'}"/>
                                <TextBlock Text="{Binding PickupPointAddress, StringFormat='Адрес пункта выдачи: {0}'}"/>
                                <TextBlock Text="{Binding OrderDate, StringFormat='Дата заказа: {0:dd.MM.yyyy}'}"/>
                            </StackPanel>

                            <Border Grid.Column="1" BorderBrush="Black" BorderThickness="1" Width="120" Height="70" Margin="10,0,0,0">
                                <TextBlock Text="{Binding DeliveryDate, StringFormat='Дата доставки: {0:dd.MM.yyyy}'}" 
                                           HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                            </Border>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>

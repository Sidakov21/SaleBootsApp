# Уп по СП SaleBootsApp

Этот документ содержит общую информацию, инструкции по установке и описание архитектуры для приложения SaleBootsApp — программы для управления складом и заказами обуви, разработанной на WPF с использованием C# и Entity Framework.

## 🚀 1. Обзор проекта

SaleBootsApp — это десктопное приложение для автоматизации учета товаров и управления заказами для сотрудников склада или магазина обуви.

**Ключевые функции:**

- **Авторизация и роли:** Поддержка ролей "Администратор", "Менеджер" и "Клиент".
- **Список товаров:** Просмотр каталога, фильтрация по поставщику, поиск по наименованию/описанию и сортировка по количеству на складе.
- **Управление товарами (только для Администратора):** Добавление, редактирование и удаление товаров.
- **Управление заказами (Администратор и Менеджер):** Просмотр списка заказов. Администратор также может добавлять, удалять и редактировать заказы.

**Стек технологий**

- **Язык:** C#
- **Фреймворк:** WPF (.NET Framework)
- **База данных:** MSSQL 

## ⚙️ 2. Установка и запуск

### 2.1. Требования

- Visual Studio (рекомендуется 2019 или новее)
- .NET Framework (версия, указанная в файле .csproj, обычно 4.7.2 или выше)
- SQL Server (или LocalDB)

### 2.2. Настройка базы данных

Предполагается, что вы используете подход Database First или LocalDB с Entity Framework.

1. **Настройте строку подключения:** Откройте файл, убедитесь, что подключайтесь к нужной бд (`DB_SP_SaleBootsEntities`).

2. **Заполните базу данных:** Убедитесь, что ваша база данных содержит необходимые таблицы (Products, Orders, Users, Roles и т.д.) и тестовые данные для работы.

### 2.3. Запуск приложения

1. Клонируйте репозиторий:
    ```bash
    git clone [ссылка на ваш репозиторий]
    ```

2. Откройте файл `SaleBootsApp.sln` в Visual Studio.
3. Соберите (Build) проект.
4. Запустите (Run) проект (клавиша F5).

## 🔑 3. Роли и доступ

Приложение использует три основные роли. Доступ к функциям определяется ролью, указанной в классе `CurrentUser.Instance`.

| Роль          | RoleID | Доступ к списку товаров | Управление товарами (Add/Edit/Delete) | Доступ к списку заказов | Управление заказами |
|---------------|--------|------------------------|---------------------------------------|------------------------|---------------------|
| Администратор | 1      | Полный                 | ✅ Да                                  | Полный                 | ✅ Да               |
| Менеджер      | 2      | Полный                 | ❌ Нет                                 | Полный                 | ❌ Нет              |
| Клиент        | 3      | Просмотр               | ❌ Нет                                 | ❌ Нет                  | ❌ Нет              |

## 📂 4. Структура основных файлов (Code-Behind)

Большая часть логики доступа и данных сосредоточена в следующих файлах:

- **`ProductListPage.xaml.cs`:**
    - Загрузка, фильтрация, поиск и сортировка товаров.
    - Метод `SetupManagerControls()` управляет видимостью элементов управления (поиск, фильтры, кнопки Администратора) в зависимости от RoleId.
    - Метод `ProductListView_MouseDoubleClick()` содержит проверку `RoleId == 1` для разрешения редактирования товаров.

- **`OrderListPage.xaml.cs`:**
    - Загрузка и отображение заказов.
    - Конструктор принимает `RoleId` для контроля доступа.
    - Метод `SetupAccessControls()` управляет видимостью кнопок добавления/удаления заказов (только для Администратора).

- **`CurrentUser.cs`:** (Предполагаемый синглтон/статический класс)
    - Хранит данные текущего авторизованного пользователя, включая RoleId и FullName.

- **`AuthPage.xaml.cs`:**
    - Логика авторизации и инициализации `CurrentUser.Instance`.
